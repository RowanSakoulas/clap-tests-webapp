# Clap Tests – Web App

Clap Tests is a browser-based acoustic analysis tool for everyday rooms.

# How to run

```bash
git clone https://github.com/RowanSakoulas/clap-tests-webapp
cd claptests
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python webapi/serve.py
